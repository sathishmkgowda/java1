package concepts;

public class stringfunctions
{
	public static void main(String[] args) 
	{
		
		
	 String	c="rajegowdru_chikku";   //taken a string shamanth ex
	 String d="bengalore_am";
	 
	
	 
	 

	 
	 System.out.println(c.length());   				 //numberofletters in string
	 System.out.println(c.charAt(4));  			 //fetch the character in a string of 4thindex
	 System.out.println(c.indexOf('c')); 			//fetch the index of the given letter
	 System.out.println(c.endsWith("ikku"));	 //boolean values
	 System.out.println(c.startsWith("sha")); 		//boolean values
	 System.out.println(c.contains("manth"));  	//boolean values
	 System.out.println(c.toUpperCase());      //converted to uppercaseletter 
	 System.out.println(c.toLowerCase());      //coverted to lowercaseletter 
	 System.out.println(c.toCharArray());     //coverted string in to CharacterArray 
	 System.out.println(c.replace('s', 'e')); //repace the perticular character in a string
	 System.out.println(c.concat("over"));    //concat over in the given string
	 System.out.println(c.substring(2, 8));    //fetchthe perticular substring in given string
	 System.out.println(c.substring(3));
	 System.out.println(c.subSequence(2, 4));
	 System.out.println(c.equals(c));         //compare object values
	 System.out.println(c==c);  			 //compare object address
	 System.out.println(c.equals(d));   //compare object values
	 System.out.println(c==d);          //compare object address
	 
	 
	 //convert string to integer
	 String a="roopesh100";
	 

/*Java String split()

Splits the string at the specified string (regex)
Java String compareTo()

Compares two strings in the dictionary order
Java String compareToIgnoreCase()

Compares two strings ignoring case differences
Java String length()

Returns the length of the string
Java String replace()

Replace all matching characters/text in the string
Java String replaceAll()

Replace all substrings matching the regex pattern
Java String substring()

Returns a substring from the given string
Java String equals()

Compares two strings
Java String equalsIgnoreCase()

Compares two strings ignoring case differences
Java String contains()

Checks whether the string contains a substring
Java String indexOf()

Returns the index of the character/substring
Java String trim()

Removes any leading and trailing whitespace
Java String charAt()

Returns the character at the given index
Java String toLowerCase()

Converts characters in the string to lower case
Java String concat()

Concatenates two strings and returns it
Java String valueOf()

Returns the string representation of a value
Java String matches()

checks whether the string matches the given regex
Java String startsWith()

Checks if the string begins with the given string
Java String endsWith()

Checks if the string ends with the given string
Java String isEmpty()

Checks whether a string is empty or not
Java String intern()

Returns a canonical representation of the string
Java String getBytes()

Encodes the string into a sequences of bytes
Java String contentEquals()

Checks whether the string is equal to charSequence
Java String hashCode()

Returns a hash code for the string
Java String join()

Joins the given strings using the delimiter
Java String replaceFirst()

Replace the first matching substring
Java String subSequence()

Returns a subsequence from the string
Java String toCharArray()

Converts the string to a char array
Java String format()

Returns a formatted string */

	 
	 

	 
	 
	 
	 
	 
	 
	 
	 
	 
		
	}

}
